# Environment Variables Template
# Copy this file to .env.local (for Next.js) or .env (for other frameworks)
# Never commit .env.local or .env to version control

# =============================================================================
# Supabase Configuration
# =============================================================================

# Local Development Values (from `supabase status` after running `supabase start`)
# Production Values: Use your Supabase project dashboard settings

# Supabase API URL
# Local: http://localhost:54321
# Production: https://<your-project-ref>.supabase.co
SUPABASE_URL=http://localhost:54321

# Supabase Anonymous/Public Key (safe for client-side use)
# Local: Retrieved from `supabase status` command
# Production: Found in Supabase Dashboard > Settings > API > anon/public key
SUPABASE_ANON_KEY=

# Supabase Service Role Key (SERVER-SIDE ONLY, never expose to client)
# Local: Retrieved from `supabase status` command
# Production: Found in Supabase Dashboard > Settings > API > service_role key
# WARNING: This key bypasses Row Level Security. Only use in Edge Functions, API routes, or server-side code.
SUPABASE_SERVICE_ROLE_KEY=

# Database Connection String (for direct Postgres access)
# Local: postgresql://postgres:postgres@localhost:54322/postgres
# Production: Found in Supabase Dashboard > Settings > Database > Connection string
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Storage API URL
# Local: http://localhost:54321/storage/v1
# Production: https://<your-project-ref>.supabase.co/storage/v1
# Note: Usually the same as SUPABASE_URL + /storage/v1
SUPABASE_STORAGE_URL=http://localhost:54321/storage/v1

# =============================================================================
# Application Configuration
# =============================================================================

# Application Base URL
# Local: http://localhost:3000 (or your dev server port)
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Application Port (if not using framework defaults)
# Next.js default: 3000
# Python/Django default: 8000
PORT=3000

# =============================================================================
# Optional: AI/LLM Configuration
# =============================================================================

# OpenAI API Key (if using OpenAI)
# OPENAI_API_KEY=

# Anthropic API Key (if using Claude)
# ANTHROPIC_API_KEY=

# =============================================================================
# Optional: Other Services
# =============================================================================

# Redis URL (if using Redis)
# REDIS_URL=redis://localhost:6379

# Email Service (if using external email provider)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=

# =============================================================================
# Instructions
# =============================================================================
#
# 1. Copy this file to .env.local (Next.js) or .env (other frameworks)
# 2. Fill in the values above, especially:
#    - Supabase keys (run `supabase status` for local values)
#    - Application URL and port
#    - Any service API keys you're using
# 3. For local Supabase setup, see: standards/architecture/supabase-local-setup.md
# 4. Never commit .env.local or .env to git

